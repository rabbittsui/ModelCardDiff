# Requirements schema for support intent model cards.
#
# One directive per line. Blank lines and lines beginning with # are ignored.
#
#   require <Section Name>   the section must exist and be non-empty
#
# Section names are matched case insensitively against the card headings.

